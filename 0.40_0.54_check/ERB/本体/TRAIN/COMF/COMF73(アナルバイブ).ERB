;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	Module		:COMFxxx.ERB
;	Facility	:TRAINコマンド(73:アナルバイブ)。
;
;	Licence		:ライセンスフリー。
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;===============================================================================
;  ◇記載関数一覧(記載順)
;
;		Module Name						Explanation
;
;	・ COM_FUNC_GET_NAME_Xxxx			取得:コマンド名称
;	・ COM_FUNC_ABLE_Xxxx				CHK:コマンド実行条件
;	・ COM_FUNC_INPUT_Xxxx				コマンド実行確認
;	・ COM_FUNC_EXE_Xxxx				コマンド実行
;	・ COM_FUNC_SRC_Xxxx				コマンドソース
;	・ COM_FUNC_TRAIN_MSG_Xxxx			コマンドメッセージ
;	・ COM_FUNC_GET_TYPE_Xxxx			取得:コマンド種別
;
;===============================================================================

;=================================================
;   sub COM_FUNC_GET_NAME_Xxxx
;=================================================
;   取得:コマンド名称
;-------------------------------------------------
; Output:
;  RESULTS				コマンド名称
;-------------------------------------------------
@COM_FUNC_GET_NAME_X73
RESULTS = 애널 바이브

;=================================================
;   sub COM_FUNC_ABLE_Xxxx
;=================================================
;   CHK:コマンド実行条件
;-------------------------------------------------
; Output:
;  RESULT				真偽値
;-------------------------------------------------
@COM_FUNC_ABLE_X73

;特定アイテムを所有
SIF !ITEM:アナルバイブ
	RETURN 0

RETURN 1

;=================================================
;   sub COM_FUNC_INPUT_Xxxx
;=================================================
;   コマンド実行確認
;-------------------------------------------------
; Output:
;  RETURN				真偽値
;-------------------------------------------------
@COM_FUNC_INPUT_X73
#DIM L_P
#DIM L_T

L_P = PLAYER
L_T = TARGET

;-----------------------------
;- 事前処理
;-----------------------------
;CHK
SIF TEQUIP:L_T:Ａ器具 != [[TRAIN:アナルバイブ]]
	RETURN 1

TEQUIP:L_T:Ａ器具 = 0

;-----------------------------
;- 情景
;-----------------------------

PRINTFORML %CALLNAME%의 항문에서 애널 바이브를 뽑았다
PRINTFORM %CALLNAME%는 
IF TALENT:음란도
	;Ａ경험180以上
	IF EXP:Ａ경험 > 180
		PRINT 음순과 입에서 침을 질질 흘리면서、여전히 엉덩이를 벌리고 있다…
	;Ａ경험140以上
	ELSEIF EXP:Ａ경험 > 140
		PRINT 촉촉한 시선으로 애절하게 엉덩이를 흔들며 조르고 있다…
	;Ａ경험100以上
	ELSEIF EXP:Ａ경험 > 100
		PRINT 아직 부족하다는 듯이 엉덩이를 쑥 내밀고 있다…
	;그 외 
	ELSE
		PRINT 살짝 아쉽다는 듯이 엉덩이를 흔들고 있다…
	ENDIF
ELSE
	;Ａ경험40以上
	IF EXP:Ａ경험 > 40
		PRINT 엉덩이를 움찔거리면서도、살짝 안도한 표정을 짓고 있다…
	;Ａ경험20以上
	ELSEIF EXP:Ａ경험 > 20
		PRINT 강제로 느낀 배설감에 뺨을 붉히고 있다…
	;그 외 
	ELSE
		PRINT 괴로운 표정으로 자극을 참고 있다…
	ENDIF
ENDIF
PRINTFORML …
WAIT


;解除だけの為、0返し
RETURN 0

;=================================================
;   sub COM_FUNC_EXE_Xxxx
;=================================================
;   コマンド実行
;-------------------------------------------------
@COM_FUNC_EXE_X73
#DIM L_P
#DIM L_T
#DIM L_TMP
#DIM L_I
VARSET LOCAL

L_P = PLAYER
L_T = TARGET

;-----------------------------
;COM別FLAGの設定
;-----------------------------

TEQUIP:L_T:Ａ器具 = [[TRAIN:アナルバイブ]]
TEQUIP:L_T:Ａ器具経過ターン = 0

L_TMP = TEQUIP:L_T:ポイント性交
IF L_TMP
	CALLFORM COM_FUNC_GET_TYPE_X{L_TMP}
	SIF RESULT == [[COM種別:Ａ性交系]]
		TEQUIP:L_T:ポイント性交 = [[TRAIN:無し]]
ENDIF

;-----------------------------
;ソース追加処理
;-----------------------------

;-----------------------------
;その他の処理
;-----------------------------


;=================================================
;   sub COM_FUNC_SRC_Xxxx
;=================================================
;   コマンドソース
;-------------------------------------------------
@COM_FUNC_SRC_X73
#DIM L_P
#DIM L_T
#DIM L_TMP
#DIM L_I
VARSET LOCAL

L_P = PLAYER
L_T = TARGET

;-----------------------------
;COM別FLAGの設定
;-----------------------------

;-----------------------------
;ソース追加処理
;-----------------------------

LOCAL:0 = 120
LOCAL:1 = 100
LOCAL:2 = 100

L_TMP=150
LOCAL:0 = LOCAL:0 * L_TMP / 100
LOCAL:1 = LOCAL:1 * L_TMP / 100
LOCAL:2 = LOCAL:2 * L_TMP / 100


;- ソース
SOURCE:L_T:快Ａ += LOCAL:0
SOURCE:L_T:性行為 += LOCAL:1
SOURCE:L_T:露出 += LOCAL:2

;- TR:ソース(部位苦痛)
CALL TR_SOURCE_ADD_部位苦痛("Ａ감각", L_T)


;-----------------------------
;経験の処理
;-----------------------------

;-----------------------------
;その他の処理
;-----------------------------



;=================================================
;   sub COM_FUNC_TRAIN_MSG_Xxxx
;=================================================
;   コマンドメッセージ
;-------------------------------------------------
@COM_FUNC_TRAIN_MSG_X73

;- 挿入時
IF !TEQUIP:Ｖ器具経過ターン

	PRINTFORM %CALLNAME%의 
	PRINTFORML 항문에 애널 바이브를 가져갔다…
	PRINTFORM %CALLNAME%는 
	IF TALENT:음란도
		;Ａ경험180以上
		IF EXP:Ａ경험 > 180
			PRINT 황홀한 표정으로、스스로 항문을 벌리고 있다
		;Ａ경험140以上
		ELSEIF EXP:Ａ경험 > 140
			PRINT 삽입을 애타게 기다리면서 허리를 실룩이고 있다
		;Ａ경험100以上
		ELSEIF EXP:Ａ경험 > 100
			PRINT 뺨을 붉히면서 기대감에 엉덩이를 살랑이고 있다
		;그 외 
		ELSE
			PRINT 항문 주름에 전해지는 바이브의 자극에 몸을 부르르 떨고 있다
		ENDIF
	ELSE
		;Ａ경험40以上
		IF EXP:Ａ경험 > 40
			PRINT 기대와 부정적인 감정이 뒤섞인 복잡한 표정을 짓고 있다
		;Ａ경험20以上
		ELSEIF EXP:Ａ경험 > 20
			PRINT 부끄러움에 물든 얼굴을 숙이고 있다
		;그 외 
		ELSE
			PRINT 더러운 구멍을 범해지는 굴욕과 치욕에 젖은 표정을 하고 있다
		ENDIF
	ENDIF
	PRINTL …
	PRINTFORM %CALLNAME%의 
	;濡れ
	SIF TCVAR:キャラ潤滑
		PRINT 점액 범벅인 
	PRINTFORML 애널에 바이브를 박아넣었다

;- 継続中
ELSE
	PRINTFORML %CALLNAME:TARGET%는 애널에 박힌 바이브가 주는 쾌감에 달콤한 교성을 올리고 있다…
ENDIF

WAIT


;=================================================
;   sub COM_FUNC_GET_TYPE_Xxxx
;=================================================
;   取得:コマンド種別
;-------------------------------------------------
; Output:
;  RESULT				コマンド種別
;-------------------------------------------------
@COM_FUNC_GET_TYPE_X73
RETURN [[COM種別:道具系]]

