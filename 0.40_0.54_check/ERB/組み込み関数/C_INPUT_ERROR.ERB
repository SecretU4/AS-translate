;=================================================
;   sub C_INPUT_ERROR
;=================================================
;   INPUTエラー対応
;-------------------------------------------------
;   入力値を消して、入力後に消える行でエラー表示
;   (INPUTエラー時の回帰処理用)
;-------------------------------------------------
; Input:
;  ARGS:0				メッセージ
;-------------------------------------------------
@C_INPUT_ERROR(ARGS="")

CLEARLINE 1
IF ARGS == ""
	REUSELASTLINE （入力値 {RESULT} は範囲外です）
ELSE
	REUSELASTLINE （%ARGS%）
ENDIF


