;=================================================
;   sub C_危険日
;=================================================
;   危険日
;-------------------------------------------------
;   対象が危険日か返す
;-------------------------------------------------
; Input:
;  ARG:0				キャラ
; Output:
;  RETURNF				真偽値
;-------------------------------------------------
@C_危険日(ARG = -1)
#FUNCTION
#DIM L_TMP
#DIM L_VAL

SIF ARG == -1
	ARG = TARGET

L_TMP = CFLAG:ARG:危険日
L_VAL = (DAY % 7)

;３日に一回危険日
L_TMP /= 2
L_VAL /= 2

IF TALENT:ARG:남자
	RETURNF 0
ELSEIF L_TMP == L_VAL
	RETURNF 1
ELSE
	RETURNF 0
ENDIF

