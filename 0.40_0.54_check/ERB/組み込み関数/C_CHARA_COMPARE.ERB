;-------------------------------------------------
;キャラ比較関数
;-------------------------------------------------
;TYPE（比較演算子）を指定して2キャラの特定変数の比較を行う汎用関数
@C_CHARA_COMPARE(VAR_NAME, VAR_ID, CHARA_A, TYPE, CHARA_B)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS VAR_NAME ;比較対象キャラ変数
#DIM  VAR_ID   ;比較対象キャラ変数のID
#DIM  CHARA_A  ;比較対象キャラAの登録番号
#DIMS TYPE     ;比較内容
#DIM  CHARA_B  ;比較対象キャラBの登録番号
SELECTCASE TYPE
CASE "=="
	RETURNF C_GET_CVAR_VALUE(CHARA_A, VAR_NAME, VAR_ID) == C_GET_CVAR_VALUE(CHARA_B, VAR_NAME, VAR_ID)
CASE ">"
	RETURNF C_GET_CVAR_VALUE(CHARA_A, VAR_NAME, VAR_ID) > C_GET_CVAR_VALUE(CHARA_B, VAR_NAME, VAR_ID)
CASE ">="
	RETURNF C_GET_CVAR_VALUE(CHARA_A, VAR_NAME, VAR_ID) >= C_GET_CVAR_VALUE(CHARA_B, VAR_NAME, VAR_ID)
CASE "<"
	RETURNF C_GET_CVAR_VALUE(CHARA_A, VAR_NAME, VAR_ID) < C_GET_CVAR_VALUE(CHARA_B, VAR_NAME, VAR_ID)
CASE "<="
	RETURNF C_GET_CVAR_VALUE(CHARA_A, VAR_NAME, VAR_ID) <= C_GET_CVAR_VALUE(CHARA_B, VAR_NAME, VAR_ID)
CASEELSE
	THROW 不明なTYPE　%TYPE%
ENDSELECT
