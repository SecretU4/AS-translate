;=================================================
;   sub C_HEADLINE
;=================================================
;   表示:見出し
;-------------------------------------------------
;   引数を元に見出し行を表示する
;-------------------------------------------------
; Input:
;  ARGS					対象値
;-------------------------------------------------
@C_HEADLINE(ARGS)
#DIM L_TMP
;PRINTFORML □ %ARGS% □---------------------------------------------------------------------

ARGS = □ %ARGS% □

L_TMP = STRLENS(ARGS)

PRINTFORM %ARGS%
PRINTFORM %"-" * (90 - L_TMP)%
PRINTL

